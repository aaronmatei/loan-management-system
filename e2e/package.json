{
  "name": "loan-tracker-e2e",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "Full-stack Playwright E2E tests (drives the real frontend + backend + an isolated e2e database).",
  "scripts": {
    "test": "node setup-db.js && playwright test",
    "pretest:headed": "node setup-db.js",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "bcryptjs": "^3.0.3",
    "dotenv": "^17.4.2",
    "pg": "^8.20.0"
  }
}
