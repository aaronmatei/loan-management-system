// Tailwind itself is handled by the @tailwindcss/vite plugin (see vite.config.js),
// so it is intentionally NOT listed here. This file only runs autoprefixer.
export default {
  plugins: {
    autoprefixer: {},
  },
};
