# Loaded automatically by `vite build` (production mode). Baked into the
# client bundle, so it's public by design — not a secret. Vercel dashboard
# env vars (if set) override this. See DEPLOYMENT.md.
VITE_API_URL=https://api.loanfix.net/api
